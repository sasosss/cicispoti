# AGENTS.md

## Cursor Cloud specific instructions

This repository (`cicispoti`) is currently an empty/skeleton repo. It contains only a `README.md` with no application code, dependencies, or services.

- **No build system**: No `package.json`, `requirements.txt`, `pyproject.toml`, `Makefile`, or similar.
- **No services to run**: No backend, frontend, database, or other infrastructure.
- **No tests or linting**: No test frameworks or linters are configured.
- **No dependencies to install**: The update script is a no-op.

When application code is added, this file should be updated with relevant setup, build, test, and run instructions.
